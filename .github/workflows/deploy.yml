name: app-ci-cd
on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Set vars
        id: vars
        run: |
          IMAGE=$(echo ghcr.io/$GITHUB_REPOSITORY | tr '[:upper:]' '[:lower:]')
          TAG=${GITHUB_SHA::7}
          echo "IMAGE=$IMAGE" >> $GITHUB_ENV
          echo "TAG=$TAG" >> $GITHUB_ENV
          echo "tag=$TAG" >> $GITHUB_OUTPUT
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & Push (latest + sha)
        run: |
          docker build -t "$IMAGE:latest" -t "$IMAGE:$TAG" .
          docker push "$IMAGE:latest"
          docker push "$IMAGE:$TAG"
    outputs:
      tag: ${{ steps.vars.outputs.tag }}

  deploy:
    needs: build_and_push
    runs-on: [self-hosted, Linux, X64]
    env:
      APP_PATH: /opt/apps/welog
    steps:
      - name: Set deployment vars (server)
        run: echo "TAG=${{ needs.build_and_push.outputs.tag }}" >> $GITHUB_ENV
      - name: Login to GHCR (server)
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Pull & Restart APP only
        run: |
          set -e
          cd "$APP_PATH"
          export TAG="$TAG"
          docker compose pull app        # ← 여기 'app'
          docker compose up -d app       # ← 여기 'app'
          docker image prune -f
